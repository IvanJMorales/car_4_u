import React from 'react';

import { Button } from '@mui/material';

const CompareButton = () => {
    return (
        <div>
            <Button />
        </div>
    );
};

export default CompareButton;